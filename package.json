{
  "name": "ecosentinel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "node --import tsx --test tests/*.test.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx db/seed.ts",
    "db:create-device": "tsx db/create-device.ts"
  },
  "//overrides": "postcss y sharp entran como dependencias transitivas de next, que las fija en versiones con CVEs (postcss <=8.5.17: XSS y lectura arbitraria de archivos vía sourceMappingURL; sharp <0.35.0: CVEs heredados de libvips). next@15.5.22 ya es la última de su línea, así que no hay actualización que lo resuelva, y `npm audit fix --force` propone DEGRADAR next a 9.3.3 (rompería el App Router entero). Estos overrides fuerzan las versiones parcheadas. sharp es de bajo riesgo aquí porque el proyecto no usa next/image.",
  "overrides": {
    "postcss": "^8.5.25",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.36.4",
    "next": "^15.1.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "resend": "^6.17.2"
  },
  "//pg": "SOLO para pruebas (devDependency, no entra en el bundle de Vercel). En producción se usa @neondatabase/serverless por HTTP; un Postgres local no habla ese protocolo, así que tests/db-local.ts sustituye el transporte del driver de Neon por `pg` para correr las rutas REALES contra la base local (ver tests/mitigate.test.ts).",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.20.0",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "dotenv": "^16.4.7",
    "drizzle-kit": "^0.28.1",
    "pg": "^8.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
